machine:
  services:
    - docker
dependencies:
 
test:
  
deployment:
  master:
    branch: master
    commands:
      - echo 'deploying'
      - ssh -v $DROPLET_USER@$DROPLET_IP "cd  /root/gocode/src/github.com/myself659/ipds-blog-website/public; git pull"